{
    "name": "qcode-ui",
    "scripts": {
        "test:jsdom": "jest --config=./test/jsdom/jest-jsdom-conf.json ./test/jsdom",
        "test:puppeteer": "jest --config=./test/headless-browser/jest-conf.json ./test/headless-browser"
    },
    "devDependencies": {
        "http-server": "^13.0.0",
        "jest": "^27.0.6",
        "jest-dev-server": "^5.0.3",
        "jest-puppeteer": "^5.0.4",
        "puppeteer": "^10.2.0",
        "puppeteer-core": "^10.2.0"
    }
}
